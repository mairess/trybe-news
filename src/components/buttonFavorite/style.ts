import styled from 'styled-components';

export const StyledButton = styled.button`
    width: 1.125rem;
    height: 1rem;
    flex-shrink: 0;
    fill: var(--Text, #2A2A2A);
    border: none;
    background: none;
    cursor: pointer;
`;
